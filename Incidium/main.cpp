#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, k, t;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        cin >> n >> k;
        if (k > n) cout << "IMPOSSIBLE" << endl;
        else {

        }
    }


    return 0;
}
